server:
  port: 8082

spring:
  application:
    name: readypath-api

management:
  endpoints:
    web:
      exposure:
        include: health,info

endpoint:
  health:
    show-details: always

info:
  application:
    name: ReadyPath AI API
    version: 0.1.0
    description: ReadyPath AI hackathon backend
